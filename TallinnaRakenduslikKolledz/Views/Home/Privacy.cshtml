@{
    ViewData["Title"] = "Privacy Policy";
}
<h1>@ViewData["Title"]</h1>

<p>Use this page to detail your site's privacy policy.</p>

@if (ViewBag.Värv == "Punane")
{
    @(Html.Display("Punane"))
}
else if (ViewBag.Värv == "Roheline")
{
    @(Html.Display("Roheline"))
}
